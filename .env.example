# Frontend env (these are exposed to the browser). Keep only non-secret keys here.
VITE_SUPABASE_URL=https://example.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key-demo
VITE_RAZORPAY_KEY_ID=rzp_test_dummy123
VITE_API_URL=https://api.example.com

# Do NOT place secrets in frontend env files; keep them on the server only:
# - SUPABASE_SERVICE_ROLE_KEY
# - RAZORPAY_KEY_SECRET
# - RAZORPAY_WEBHOOK_SECRET
# - Any database credentials or JWT secrets